"""Feedback handling (v0.2 skeleton)."""

from typing import Dict


def apply_feedback(command: str, cfg: Dict) -> str:
    """Apply like/dislike/source_up etc."""
    # In real impl this would call feedback_update.py logic
    return f"feedback applied: {command} (v0.2 stub)"